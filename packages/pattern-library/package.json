{
  "name": "@isallitis/pattern-library",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "healthcheck": "npm run types:check && npm run lint && npm run test && npm run build",
    "lint": "eslint",
    "types:check": "tsc"
  },
  "description": "Pattern library for *Is All It Is* applications.",
  "dependencies": {
    "@preact/signals": "^1.3.0"
  }
}
