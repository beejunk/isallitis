{
  "name": "@isallitis/to-do",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "healthcheck": "npm run types:check && npm run lint && npm run test && npm run build",
    "lint": "eslint",
    "types:check": "tsc",
    "start": "nodemon -e js,css,html dev-server/start.js"
  },
  "description": "A to-do app is all it is.",
  "dependencies": {
    "@isallitis/shared-components": "workspace:*",
    "@preact/signals-core": "^1.11.0"
  },
  "devDependencies": {
    "@fastify/static": "^8.2.0",
    "fastify": "5.4.0"
  }
}
